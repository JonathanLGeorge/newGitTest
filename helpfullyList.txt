/*git CMD
type git to get a list of commands

cd  // change directory
cd ..
cd jonathan/document
ls  //list all files in directory
dir // if on windows shows files in directory

step one
git clone https://github.com/JonathanLGeorge/newGitTest.git

now we can add stuff to the folders

git status  //shows us files that may need to be added?

git add nameOfFile// adds any changes or files by name example: add index.html
git add .
git add .A

git commit -m "mesage"

git push
git pull

merg conflicts happen
start work by doing 
git pull
push your changes
git add .A
git commit -m "hey this is what i changed"
git pull or push 
if merg conflict look at code make changes, delet helper stuff save file 
git add .add
git commit -m "what you did"
git push
:)
if stuck press
esc 
:wq
*/